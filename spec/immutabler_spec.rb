require 'spec_helper'

describe Immutabler do
  it 'has a version number' do
    expect(Immutabler::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
